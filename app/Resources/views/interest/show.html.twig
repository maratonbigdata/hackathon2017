{% extends 'base.html.twig' %}

{% block body %}
    <h1>Interest</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interest.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interest.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('interest_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('interest_edit', { 'id': interest.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
