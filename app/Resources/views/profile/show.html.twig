{% extends 'base.html.twig' %}

{% block body %}
    <h1>Perfil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ profile.name }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ profile.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profile_index') }}" class="btn btn-info btn1" role="button" >Perfiles</a>

    <a href="{{ path('profile_edit', { 'id': profile.id }) }}" class="btn btn-primary btn2" role="button">Editar</a>

    <input type="submit" value="Eliminar" class="btn btn-danger btn2" role="button">
    
{% endblock %}
